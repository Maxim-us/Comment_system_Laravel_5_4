Comment system for the blog on Laravel 5.4.

Supports endless children's comments.

For example, a simple blog with models and controllers is created.

There is already a system of authorization.

To start, run the "$ php artisan migrate" command.
After that, the following tables will appear in the database: blog, comments, migrations, password_resets, users.